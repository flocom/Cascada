---
- hosts: all
  roles:
    - role: ea31337.metatrader
      vars:
        # XM Global's MT4 setup — MetaQuotes hosts it on their CDN and
        # EA31337 uses it in their own CI (MetaQuotes stopped hosting
        # a generic mt4setup.exe years ago).
        metatrader_setup_url: https://download.mql5.com/cdn/web/3315/mt4/xm4setup.exe
        metatrader_version: 4
